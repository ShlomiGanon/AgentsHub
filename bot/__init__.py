"""Telegram Frontend (work_plan.md §8).

Entry point is `bot.app` — see docs/allowed_calls.md. Built against a
Mission 7 (API Layer) that does not exist yet; see `bot/api_client.py`
and `docs/progress.md`'s Mission 8 entry for the seam that stands in for
it and exactly what depends on it.
"""
