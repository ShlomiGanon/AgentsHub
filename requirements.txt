# Runtime dependencies for the Field-Report Multi-Agent System.
#
# Foundations (work_plan.md §1) and the Data Layer (§2) need nothing
# beyond the Python standard library.
#
# Agent Framework (§3): crewai is not installed in this environment yet —
# confirmed with the user. agents/adapter.py is built and tested against
# its documented API (docs.crewai.com, checked 2026-08-23, describing
# roughly the v1.13-1.15 series) via a monkeypatched fake; see
# docs/progress.md's §3.5 entry for exactly what was verified and what
# wasn't. Uncomment the line below (and re-verify the version/API against
# whatever is current at the time) once real model API keys are available.
#
# crewai>=1.13

# Telegram Frontend (§8): unlike crewai, this needs no model API key to
# install or to exercise its non-network code paths, so it's a real,
# uncommented dependency — installed and used directly by
# bot/telegram_client.py. See docs/progress.md's Mission 8 entry for what
# is, and isn't, verified against a live Telegram bot token.
python-telegram-bot==22.8
