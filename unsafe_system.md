# תוכנית מעבר בין מצב בטוח למצב פתוח

## 1. מטרה

לאפשר לבוט Telegram לקבל פניות מכל אדם ומכל קבוצה כאשר `safe_mode=false`, תוך רישום אוטומטי ומוגבל שלהם כ־`VIEWER`, ולחסום ישויות שנקלטו אוטומטית כאשר `safe_mode=true` עד לאישור ידני.

המצב הפתוח חל רק על ערוץ Telegram. הוא אינו הופך את מנגנון האימות הכללי של ה־API לפתוח ואינו מאפשר לזהות לא מוכרת לקבל הרשאות באמצעות `X-Identity` בלבד.

## 2. החלטות מחייבות

- `safe_mode=false` הוא ערך ברירת המחדל בהתקנה חדשה ובמערכת קיימת.
- במצב פתוח, משתמש Telegram לא מוכר נרשם כ־`VIEWER` עם `auto_register=true`, ללא שדה או שיוך סוכן ברמת המשתמש.
- במצב פתוח, קבוצת Telegram לא מוכרת נרשמת עם `auto_register=true` ועם שיוך ל־`main_agent`.
- כל הודעה בשיחה פרטית עם הבוט מנותבת תמיד ל־`main_agent`, ללא תלות בזהות המשתמש או באופן שבו נרשם.
- אדם שנרשם אוטומטית חייב למסור שם מלא תקין לפני המשך הפעולה המקורית.
- במעבר ל־`safe_mode=true`, משתמשים וקבוצות עם `auto_register=true` חסומים עד לאישור מפורש.
- אישור מפורש משנה את `auto_register` ל־`false` ואינו משנה הרשאה, שם או שיוך של קבוצה.
- `safe_mode` נחשף דרך `GET /SYSTEM` רק ל־`COMMANDER`.
- רק `COMMANDER` רשאי לשנות את `safe_mode` דרך `PUT /SYSTEM`.
- פנייה חיצונית עם `X-Identity` לא מוכר ממשיכה לקבל שגיאת אימות בכל מצב.

## 3. כללי אבטחה שאסור להפר

1. אין לשנות את `authenticate()` כך שזהות לא מוכרת תקבל `VIEWER` באופן כללי.
2. רישום אוטומטי יתאפשר רק דרך זרימה ייעודית של הבוט, המזוהה באמצעות `bot-service` וגם `X-Service-Key` תקין.
3. גוף בקשה, `X-Identity`, מזהה Telegram או שדה המציין לכאורה שהמקור הוא Telegram אינם הוכחה מספקת לבדם.
4. רישום משתמש וקבוצה ובדיקת `safe_mode` יבוצעו בצד השרת, ולא כהחלטת אמון מקומית של הבוט.
5. הרשאת משתמש שנקלט אוטומטית תהיה תמיד `viewer`; אסור לקבל את רמת ההרשאה מהבקשה.
6. בשיחה פרטית יעד הניתוב יהיה תמיד `main_agent`; אין שדה שיוך למשתמש ואין לאפשר ללקוח לבחור יעד אחר.
7. שיוך ברירת המחדל לקבוצה אוטומטית יהיה תמיד `main_agent`; אסור לקבל שיוך אחר בבקשת הרישום האוטומטי.
8. שינוי ידני של פרטי משתמש או קבוצה לא יאשר אותם במקרה. רק פעולת אישור ייעודית תשנה את `auto_register` ל־`false`.
9. יש להגן גם מפני מעבר מצב שמתרחש בין קליטת העדכון ב־Telegram לבין שליחת הפעולה העסקית ל־API.
10. כל רישום אוטומטי, חסימה, אישור ושינוי מצב יירשמו ביומן מובנה ללא שמירת סודות.

## 4. מודל נתונים ומיגרציות

### 4.1 טבלת `users`

להוסיף את השדות:

```sql
auto_register INTEGER NOT NULL DEFAULT 0 CHECK (auto_register IN (0, 1));
```

משמעות השדות:

- `auto_register=1`: המשתמש נוצר אוטומטית מפניית Telegram ועדיין לא אושר.
- `auto_register=0`: המשתמש נוצר או אושר ידנית ונחשב מוכר במצב בטוח.

אין להוסיף ל־`users` עמודת `agent_name` או כל שדה ניתוב מקביל. זהות המשתמש אינה משתתפת בהחלטת הניתוב של שיחה פרטית, שתמיד מגיעה ל־`main_agent`.

המיגרציה תסמן את כל המשתמשים הקיימים ב־`auto_register=0`, כדי לא לחסום משתמשים שהיו מוכרים לפני השדרוג. `bot-service` חייב להישאר רשומת `COMMANDER` ידנית ולא יוכל להפוך לרשומה אוטומטית.

### 4.2 טבלת `telegram_groups`

להוסיף:

```sql
auto_register INTEGER NOT NULL DEFAULT 0 CHECK (auto_register IN (0, 1));
```

העמודה הקיימת `agent_name` נשארת מקור שיוך הסוכן. קבוצות קיימות יקבלו `auto_register=0`. קבוצה חדשה שנקלטת אוטומטית תקבל `agent_name='main_agent'` ותווית המבוססת על שם הקבוצה מ־Telegram אם הוא זמין; בהיעדר שם תישמר תווית ריקה.

### 4.3 חוזי התמדה

לעדכן את `PersistenceInterface` ואת `SQLitePersistence` כך שפעולות הקריאה והרשימות יחזירו את השדות החדשים.

יש להפריד בין הפעולות הבאות:

- יצירה ידנית של משתמש או קבוצה: `auto_register=false`.
- רישום אוטומטי מותנה: יוצר רשומה רק אם היא חסרה, תמיד עם `auto_register=true`.
- עריכת שם או הרשאת משתמש, וכן עריכת תווית או סוכן של קבוצה: משמרת את ערך `auto_register` הקיים.
- אישור: משנה באופן ממוקד רק את `auto_register` ל־`false`.

נדרשות פעולות התמדה אטומיות כגון:

- `register_telegram_user_if_missing(identity)`.
- `register_telegram_group_if_missing(chat_id, label)`.
- `approve_user(identity)`.
- `approve_group(chat_id)`.

יש להשתמש ב־transaction וב־`INSERT ... ON CONFLICT DO NOTHING` כדי ששני עדכוני Telegram מקבילים לא ידרסו שם, הרשאה, שיוך קבוצה או אישור ידני.

### 4.4 אובייקטי תצוגה ומטמון

להרחיב את `UserLookupResult`, `GroupBindingView` ו־`GroupBinding` עם `auto_register`. אין להוסיף שיוך סוכן ל־`UserLookupResult`.

כל מטמון משתמשים או קבוצות חייב לכלול את השדות החדשים. אישור, מחיקה, רישום אוטומטי או שינוי מצב חייבים להביא לביטול המטמון המתאים. אין להסתמך על TTL של 60 שניות לצורך אכיפת מעבר למצב בטוח.

## 5. שמירת `safe_mode`

להוסיף ל־`SettingsStore` ערך בוליאני מתמשך בשם `safe_mode`.

- ברירת המחדל היא `false`.
- קובץ הגדרות קיים שאין בו את השדה ישודרג באמצעות `setdefault("safe_mode", false)` ויישמר מחדש באופן אטומי.
- נדרשות הפעולות `get_safe_mode()` ו־`set_safe_mode(value)`.
- יש לאמת שהערך הוא `bool` ממש; אין לקבל `0`, `1`, מחרוזות או `null`.
- איפוס מסד והגדרות ייצור מחדש `safe_mode=false`.
- החלפת פרופיל תטען את הערך מקובץ ההגדרות של הפרופיל הפעיל, בהתאם לבידוד הקיים בין פרופילים.

כדי לסגור מרוץ בין שינוי מצב לפעולה שכבר התחילה, בדיקת הקבלה תתבצע מחדש בגבול ה־API לפני כל פעולה שמקורה ב־Telegram. אין להסתפק בערך שנשמר בבוט.

## 6. חוזה `GET /SYSTEM` ו־`PUT /SYSTEM`

### 6.1 `GET /SYSTEM`

עבור `COMMANDER`, להוסיף את השדה בתוך אובייקט ההגדרות הקיים:

```json
{
  "settings": {
    "retry_count": 3,
    "risk_threshold": 0.5,
    "lookback_window_days": 30,
    "safe_mode": false
  }
}
```

עבור `VIEWER`, `safe_mode` לא יוחזר כלל. יש לשמור את ההתנהגות הקיימת שבה גם יתר ההגדרות הפנימיות אינן נחשפות למשתמש שאינו מורשה.

### 6.2 `PUT /SYSTEM`

להוסיף את `safe_mode` לרשימת השדות המותרים. דוגמה:

```json
{
  "safe_mode": true
}
```

כללי הטיפול:

- האימות וההרשאה הקיימים של `CHANGE_SETTINGS` נשארים; בפועל רק `COMMANDER` מורשה.
- ערך שאינו בוליאני מחזיר שגיאת קלט עם `field="safe_mode"` ואינו משנה אף הגדרה.
- בקשה הכוללת כמה הגדרות תאומת במלואה לפני כתיבה, כדי למנוע עדכון חלקי.
- התשובה תחזיר את תמונת ההגדרות המעודכנת, כולל `safe_mode`, ל־`COMMANDER`.
- שינוי מצב אינו דורש אתחול API או בוט.
- שינוי ל־`true` נכנס לתוקף מיד ואינו מוחק רשומות אוטומטיות.
- שינוי ל־`false` מאפשר שוב לישויות האוטומטיות הקיימות לפנות לבוט, בלי ליצור אותן מחדש.
- יש לכתוב אירוע audit הכולל מצב קודם, מצב חדש וזהות המבצע.

## 7. שער קבלה ייעודי ל־Telegram

להוסיף שירות ונתיב פנימי ייעודי לבדיקת כל עדכון Telegram לפני טיפול עסקי. שם הנתיב המדויק יכול להיקבע לפי מוסכמות ה־API, אך החוזה חייב להיות ייעודי ולא חלק מ־`authenticate()` הכללי.

הנתיב יקבל מהבוט:

- מזהה המשתמש מ־Telegram.
- סוג השיחה: `private`, `group` או `supergroup`.
- מזהה השיחה.
- שם הקבוצה, אם Telegram מספק אותו.

הנתיב יהיה נגיש רק ל־`bot-service` עם `X-Service-Key` תקין, ויחזיר תוצאה סמנטית שאינה מאלצת את הבוט לנחש מדיניות:

- `allowed`.
- `reason`: מוכר, נרשם אוטומטית, חסום במצב בטוח, או קבוצה חסומה.
- רשומת המשתמש לאחר הקבלה, כולל הרשאה, שם ו־`auto_register` בלבד.
- רשומת הקבוצה כאשר מדובר בשיחה קבוצתית.

### 7.1 אלגוריתם קבלה אטומי

1. לאמת `bot-service` ו־`X-Service-Key`.
2. לקרוא את `safe_mode` העדכני.
3. לבדוק את המשתמש ואת הקבוצה, אם קיימת.
4. אם `safe_mode=true`:
   - משתמש חסר או בעל `auto_register=true` אינו מורשה.
   - קבוצה חסרה או בעלת `auto_register=true` אינה מורשית.
   - לא ליצור רשומות חדשות.
5. אם `safe_mode=false`:
   - ליצור משתמש חסר כ־`viewer`, עם שם ריק ו־`auto_register=true`, ללא שדה ניתוב.
   - ליצור קבוצה חסרה עם `auto_register=true` ו־`agent_name='main_agent'`.
   - לא לשנות רשומה קיימת ולא להחזיר הרשאה קיימת ל־`viewer`.
6. בשיחה קבוצתית, גם המשתמש וגם הקבוצה חייבים לעבור את השער.
7. להחזיר את הרשומות האפקטיביות ואת החלטת הקבלה.

## 8. אכיפה בגבול ה־API

הרישום האוטומטי מתבצע רק בנתיב השירות הייעודי. כל יתר הנתיבים ממשיכים לדרוש זהות קיימת.

בנוסף, כאשר `safe_mode=true`, יש לחסום רשומה עם `auto_register=true` לפני ביצוע פעולה שמקורה ב־Telegram. האכיפה תתבצע בשכבת boundary משותפת ולא רק ב־UI של הבוט, כדי שמעבר מצב בין בדיקת הקבלה לבין `POST /Msg` לא יאפשר פעולה נוספת.

לשם הבחנה מהימנה בין פנייה מהבוט לבין API חיצוני, הבוט יוסיף הוכחת שירות חתומה גם כאשר `X-Identity` הוא זהות המשתמש. אפשר להשתמש ב־`X-Service-Key` הקיים לצד זהות המשתמש, והשרת יבדוק אותו רק לצורך סימון המקור כ־Telegram. לקוח חיצוני שאינו מחזיק במפתח לא יוכל להפעיל מסלול רישום או לעקוף חסימת מצב בטוח.

כללים:

- `X-Identity` לא מוכר ללא מסלול השירות הייעודי: `401` בכל מצב.
- זהות שנרשמה אוטומטית אינה מקבלת יותר מהרשאות `VIEWER`.
- `COMMANDER`, משתמש ידני ו־bot-service אינם נחסמים במעבר ל־`safe_mode=true`.
- הרשאת הפעולה הרגילה נבדקת לאחר בדיקת הקבלה; מצב פתוח אינו מרחיב את `ViewerAllowedAction`.
- `sender_identity` עדיין חייב להיות זהה ל־`X-Identity`.
- קבוצת Telegram אוטומטית חסומה במצב בטוח גם אם המשתמש אושר, ולהפך.

## 9. זרימת הבוט

יש להעביר כל command, הודעת טקסט ו־callback דרך שער הקבלה לפני כל פעולה אחרת.

### 9.1 שיחה פרטית במצב פתוח

1. מתקבל עדכון Telegram.
2. הבוט שולח בקשת קבלה מאומתת לשרת.
3. משתמש חסר נוצר כ־`VIEWER`, `auto_register=true`, עם `full_name=''` וללא שיוך סוכן.
4. הבוט שומר את הפעולה המקורית ומבקש שם מלא.
5. תשובה תקינה נשמרת בשדה `full_name` של אותו משתמש.
6. תשובת השם אינה נשלחת לסוכנים.
7. הפעולה המקורית ממשיכה בדיוק פעם אחת.

### 9.2 קבוצה לא רשומה במצב פתוח

1. השרת יוצר את הקבוצה עם `main_agent` ו־`auto_register=true`.
2. השרת יוצר את השולח אם אינו קיים.
3. אם לשולח אין שם מלא, הבוט מבקש אותו באותה שיחה.
4. לאחר שמירת השם, הפעולה המקורית ממשיכה פעם אחת בקבוצה.
5. ניתוב ההודעה נקבע לפי שיוך הקבוצה בלבד.

### 9.3 מצב בטוח

- משתמש חסר או אוטומטי בשיחה פרטית יקבל הודעת סירוב שאינה חושפת מידע על משתמשים אחרים.
- קבוצה חסרה או אוטומטית תטופל בהתאם להתנהגות השקטה הקיימת לקבוצה לא מורשית: אין תשובה עסקית בקבוצה, ונכתב log מובנה.
- משתמש ידני בתוך קבוצה אוטומטית עדיין חסום.
- משתמש אוטומטי בתוך קבוצה ידנית עדיין חסום.
- אין למחוק שם, היסטוריה או נתוני רישום בעת החסימה.

### 9.4 קליטת שם ומצבים תלויים

להשתמש בכללי אימות השם הקיימים: לפחות שתי מילים, נרמול רווחים, ללא תווי בקרה ובמגבלת האורך הקיימת.

מפתח הפעולה הממתינה צריך לכלול לפחות `(telegram_identity, chat_id)` כדי שתשובת שם בקבוצה אחת לא תחדש פעולה שהתחילה בשיחה אחרת. יש לטפל בניקוי הפעולה במקרה של timeout, מחיקה, חסימה עקב שינוי מצב או אתחול הבוט.

לפני חידוש הפעולה לאחר שמירת השם יש לבצע שוב את שער הקבלה. אם בינתיים הופעל `safe_mode=true`, הפעולה לא תחודש עד לאישור ידני.

### 9.5 סדר ניתוב לסוכן

- בשיחה קבוצתית: `telegram_groups.agent_name` קובע.
- בשיחה פרטית: היעד הוא תמיד `main_agent`.
- אין לקרוא שיוך סוכן מרשומת משתמש, מהבקשה, מהפרופיל או מכל ברירת מחדל הניתנת לשינוי.
- אין להציג בממשק או ב־CLI אפשרות לבחור סוכן עבור משתמש.
- `main_agent` פירושו ניתוב מלא ולא מצומצם.
- כל שיוך אחר של קבוצה חייב לעבור את allowlist הקיים של סוכנים הניתנים לניתוב.
- מחיקת סוכן מפרופיל מחייבת טיפול ברשומות המצביעות אליו לפי מדיניות אימות הפרופיל הקיימת; אין לבצע fallback שקט לסוכן אחר.

## 10. אישור ידני וניהול

### 10.1 משתמשים

בעמוד ניהול המשתמשים להציג:

- מזהה Telegram.
- שם מלא.
- הרשאה.
- מקור רישום: אוטומטי או מאושר.
- מצב נוכחי: פעיל או חסום בהתאם ל־`safe_mode` ול־`auto_register`.

לרשומה אוטומטית יופיע כפתור **אישור משתמש**. הפעולה דורשת session תקין, CSRF ואישור מפורש, ומשנה רק `auto_register=false`.

עריכת שם או הרשאה אינה מאשרת את המשתמש אוטומטית. יצירת משתמש ידנית יוצרת אותו עם `auto_register=false`. אין להציג או לשמור שיוך סוכן למשתמש.

### 10.2 קבוצות

בעמוד ניהול הקבוצות להציג את אותם פרטי מקור ומצב לצד התווית ושיוך הסוכן. לקבוצה אוטומטית יופיע כפתור **אישור קבוצה**.

עריכת תווית או שיוך סוכן אינה אישור. יצירת קבוצה ידנית יוצרת אותה עם `auto_register=false`.

### 10.3 אישור בידי `COMMANDER`

להוסיף פעולות API מאומתות וממוקדות לאישור משתמש ולאישור קבוצה. הן יהיו מורשות רק ל־`COMMANDER`, יהיו idempotent, יחזירו את הרשומה המעודכנת ויתועדו ב־audit log.

ממשק האדמין ישתמש בשירותי הדומיין המשותפים לאותן פעולות, כדי שלא יהיו שתי משמעויות שונות ל־“אישור”. אין לאפשר ל־`VIEWER` לאשר את עצמו או קבוצה.

### 10.4 הגדרת מצב בממשק

בעמוד ניהול השרת/הגדרות המערכת להציג מתג ברור עבור מצב בטוח, ללא הצגת מונחי HTTP למשתמש:

- מצב פתוח: הבוט מקבל ורושם אנשים וקבוצות חדשים.
- מצב בטוח: רק משתמשים וקבוצות שאושרו ידנית פעילים.

לפני מעבר למצב בטוח יוצג מספר המשתמשים והקבוצות האוטומטיים שייחסמו. השינוי יבוצע דרך `PUT /SYSTEM`, ויוצג חיווי הצלחה או שגיאה ידידותי.

המימוש מציג את הערך החי ואת המתג בדף **ניהול השרת**, עם מעבר מונפש ונגיש בין המצבים. העדכון נכנס לתוקף מיד בשרת הפעיל ואינו דורש אתחול.

## 11. חוזי API של משתמשים וקבוצות

יש לעדכן את ייצוגי המשתמשים והקבוצות כך ש־`COMMANDER` וכלי הניהול יוכלו לראות את `auto_register`. השדה `agent_name` שייך לייצוג קבוצה בלבד.

- `GET /User/<identity>`: `COMMANDER` יקבל את כל שדות הניהול; `VIEWER` הבודק את עצמו לא יקבל מידע ניהולי שאינו נחוץ לפעולת הבוט.
- `GET /Groups`: יכלול `auto_register` עבור `COMMANDER`/`bot-service` לצורך ניהול ובקרת הקבלה.
- כתיבות ידניות ישמרו `auto_register` אם השדה לא נשלח.
- אין לקבל `auto_register=true` מלקוח ניהול רגיל כדרך ליצירת זהות “אוטומטית”; הדגל `true` נוצר רק בזרימת הקבלה של Telegram.
- אישור משתמש וקבוצה יתבצע בפעולה ייעודית ולא בעדכון חופשי של השדה.

יש לעדכן גם את תיעוד ה־endpoint registry ואת בדיקות הכיסוי של כל נתיב חדש.

## 12. תצפיות, Audit והגנת פרטיות

להוסיף אירועים מובנים לפחות עבור:

- `telegram_user_auto_registered`.
- `telegram_group_auto_registered`.
- `telegram_admission_denied_safe_mode`.
- `telegram_user_approved`.
- `telegram_group_approved`.
- `safe_mode_changed`.

כל אירוע יכלול trace ID, מזהה הישות הרלוונטית וסיבת ההחלטה. שינוי מצב ואישור יכללו את זהות ה־COMMANDER או את מזהה session האדמין. אין לרשום `X-Service-Key`, token של Telegram או תוכן הודעה כחלק מאירועי האבטחה האלה.

יש להוסיף מדדים למספר רישומים אוטומטיים, חסימות ואישורים, ולהציג באדמין ספירות בלבד בלי לחשוף מידע למשתמשי `VIEWER`.

## 13. תאימות לאחור ועדכון רכיבים

לעדכן יחד את הרכיבים הבאים:

- סכמת SQLite ומנגנון המיגרציות.
- `PersistenceInterface`, מימוש SQLite וה־fakes בבדיקות.
- `SettingsStore` ובניית התלויות ב־API.
- שכבת האימות וההרשאה בגבול הבקשה.
- נתיבי `/SYSTEM`, משתמשים, קבוצות ונתיב הקבלה הפנימי.
- `GroupRoutingTable` וחוזי ה־bot/API.
- `HttpApiClient` של הבוט כך שישלח את הוכחת השירות גם בשם משתמש Telegram.
- כל handlers של command, טקסט ו־callback.
- מסכי משתמשים, קבוצות וניהול השרת והתרגומים לעברית/אנגלית.
- CLI לניהול משתמשים וקבוצות, כולל תצוגת מקור הרישום ופעולת אישור מפורשת.
- README, קטלוג הקבצים, תיעוד הרשאות ומטריצת הפעולות.

## 14. סדר יישום

1. להוסיף בדיקות חוזה כושלות עבור ההחלטות והאינווריאנטים האבטחתיים.
2. להוסיף מיגרציות ושדות לחוזי ההתמדה.
3. להוסיף `safe_mode` ל־`SettingsStore` עם שדרוג קובץ קיים וברירת מחדל `false`.
4. להרחיב את `GET/PUT /SYSTEM` ולבצע אימות אטומי של payload.
5. לממש שירות קבלה אטומי ונתיב פנימי המוגן ב־bot-service key.
6. להוסיף אכיפת Telegram חוזרת בגבול הפעולות העסקיות.
7. לעדכן את הבוט, חוזי התעבורה, המטמונים וקליטת השם.
8. להוסיף ניתוב משתמש פרטי ושדות `auto_register` לניתוב קבוצות.
9. להוסיף פעולות אישור ל־COMMANDER ולממשק האדמין.
10. לעדכן CLI, תרגומים ותיעוד.
11. להריץ את כל הבדיקות בצורה מרוכזת רק לאחר השלמת היישום.

## 15. תוכנית בדיקות

### 15.1 מיגרציות והתמדה

- מסד ישן מקבל את העמודות החדשות בלי אובדן נתונים.
- כל המשתמשים והקבוצות הישנים מקבלים `auto_register=false`.
- סכמת המשתמשים אינה מכילה `agent_name`, וכל שיחה פרטית מנותבת בפועל ל־`main_agent`.
- יצירה אוטומטית מקבילה של אותה ישות יוצרת רשומה אחת בלבד.
- יצירה אוטומטית אינה דורסת הרשאה, שם, שיוך קבוצה או אישור קיימים.
- עריכה רגילה משמרת `auto_register`; אישור משנה רק אותו.
- קובץ הגדרות ישן מקבל `safe_mode=false` בלי לאבד ערכים אחרים.

### 15.2 `/SYSTEM`

- `COMMANDER` רואה `settings.safe_mode`; `VIEWER` אינו רואה אותו.
- `COMMANDER` יכול לשנות `true` ו־`false` ללא אתחול.
- `VIEWER`, משתמש לא מוכר וזהות שירות ללא מפתח תקין אינם יכולים לשנות אותו.
- ערכים כמו `0`, `1`, `"false"`, `null`, רשימה או אובייקט נדחים.
- payload מרובה שדות אינו נכתב חלקית כאשר שדה אחד אינו תקין.
- הערך נשמר לאחר אתחול ונפרד בין פרופילים.

### 15.3 API ואבטחה

- `X-Identity` לא מוכר נדחה בכל endpoint רגיל גם כאשר `safe_mode=false`.
- נתיב הקבלה נדחה ללא `bot-service` ומפתח שירות תקין.
- לקוח אינו יכול לבחור הרשאת `COMMANDER`, ואינו יכול לבחור סוכן למשתמש או סוכן שאינו `main_agent` לקבוצה בזמן auto-registration.
- `sender_identity` שונה מ־`X-Identity` עדיין נדחה.
- שינוי מצב באמצע זרימה נחסם בבדיקה השנייה לפני הפעולה העסקית.
- משתמש/קבוצה אוטומטיים חסומים במצב בטוח עד אישור.
- אישור עצמי בידי `VIEWER` נדחה.

### 15.4 בוט Telegram

- משתמש זר במצב פתוח נרשם כ־VIEWER ומקבל בקשת שם.
- שם לא תקין מביא לבקשה חוזרת ללא ביצוע הפעולה.
- שם תקין נשמר והפעולה המקורית נמשכת פעם אחת בלבד.
- תשובת השם אינה מגיעה לסוכן ואינה נרשמת כאירוע עסקי.
- command, הודעה ו־callback עוברים את אותה מדיניות.
- קבוצה זרה נרשמת עם `main_agent` ועם `auto_register=true`.
- כל הודעה פרטית מגיעה ל־`main_agent`, גם לאחר עריכת המשתמש או אישורו.
- בקבוצה זרה, גם הקבוצה וגם האדם נרשמים באופן אטומי ככל האפשר.
- משתמש אוטומטי חסום בקבוצה ידנית במצב בטוח.
- משתמש ידני חסום בקבוצה אוטומטית במצב בטוח.
- פעולת שם ממתינה אינה זולגת בין צ'אטים.
- מעבר למצב בטוח בזמן בקשת שם מונע את חידוש הפעולה.
- משתמש וקבוצה מאושרים ממשיכים לעבוד במצב בטוח.

### 15.5 ניהול ו־CLI

- הרשימות מציגות מקור רישום ומצב פעיל/חסום.
- כפתור אישור מופיע רק כשנדרש ופועל באופן idempotent.
- עריכה רגילה אינה מאשרת בטעות.
- כל פעולות הכתיבה מוגנות ב־session וב־CSRF.
- מתג המצב משתמש ב־`/SYSTEM` ומציג את השינוי בלי מונחי GET/PUT בממשק.
- ממשקי עברית ואנגלית מציגים טקסט מלא וללא זליגת שפה.

### 15.6 רגרסיה מלאה

- להריץ את כל חבילת `pytest` לאחר השלמת כל השלבים.
- לבדוק אתחול API ובוט עם מסד חדש ועם מסד שעבר מיגרציה.
- לבדוק איפוס מסד, החלפת פרופיל וחזרה מפרופיל שכשל.
- לבצע בדיקת Telegram ידנית מבוקרת בארבעת המצבים: אדם חדש בפרטי, אדם חדש בקבוצה חדשה, מעבר למצב בטוח, ואישור דרך האדמין.

## 16. קריטריוני קבלה

העבודה תיחשב שלמה רק כאשר מתקיימים כולם:

- במצב פתוח, אדם וקבוצה חדשים יכולים להתחיל זרימה דרך Telegram בלבד.
- האדם אינו ממשיך מעבר לבקשת השם עד לשמירת שם מלא תקין.
- הרשומות האוטומטיות מסומנות ואינן נחשבות מאושרות.
- מעבר למצב בטוח חוסם אותן מיד בלי למחוק אותן.
- אישור ידני מחזיר גישה במצב בטוח ואינו משנה נתונים אחרים.
- API חיצוני אינו רושם או מאשר זהות לא מוכרת בשום מצב.
- `safe_mode` נגיש לשינוי ולצפייה רק בהתאם להרשאות שנקבעו.
- כל מסכי הניהול מציגים פעולות שימושיות ולא פרטי HTTP טכניים.
- אין עקיפת הרשאה דרך cache, מרוץ, התחזות ל־Telegram או payload שניתן לשליטה.
- כלל הבדיקות עובר לאחר היישום.

## 17. מחוץ להיקף

- אין להעניק לאדם זר הרשאת `COMMANDER`.
- אין לפתוח endpoint רגיל לזהות לא רשומה.
- אין לבצע אימות חיצוני של Telegram ID מול Telegram מעבר לעדכון שהבוט קיבל בפועל.
- אין למחוק אוטומטית רשומות שלא אושרו.
- אין לאשר ישות אוטומטית רק משום שנערכו שמה, הרשאתה, התווית שלה או שיוך הקבוצה שלה.
- אין להוסיף שיוך סוכן למשתמש; ניתוב פרטי ל־`main_agent` הוא כלל קשיח.
- אין לטעון נתוני seed לאחר איפוס רק כדי לשנות את ברירת המחדל של `safe_mode`.
